{
  "name": "ecis-solutions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-with-cache-bust.js",
    "build:prod": "node scripts/build-with-cache-bust.js && docker compose down && docker compose up -d --build",
    "build:force": "node scripts/build-with-cache-bust.js && docker compose down && docker compose up -d --build --force-recreate",
    "build:clean": "docker compose down && docker system prune -f && vite build && docker compose up -d --build",
    "deploy": "npm run build:prod",
    "preview": "vite preview"
  },
  "dependencies": {
    "@heroicons/vue": "^2.2.0",
    "pinia": "^3.0.3",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "@vitejs/plugin-vue": "^6.0.1",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "sharp": "^0.34.4",
    "tailwindcss": "^3.4.0",
    "vite": "^7.0.6",
    "vite-plugin-vue-devtools": "^8.0.0"
  }
}
